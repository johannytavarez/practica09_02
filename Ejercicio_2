using System;

class numeros {
  public static void Main (string[] args) {

    Console.WriteLine("Este programa escribe en consola los numeros del 1 al 10");
    int numero;
    numero = 1;
    do{
      Console.WriteLine(numero);
      numero = numero+1;

    } while (numero<=10);

  }
}
